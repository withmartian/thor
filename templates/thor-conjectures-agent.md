# Agent Conjectures (thor-conjectures-agent.md)

Agent-owned. The agent writes and maintains this file, updating it after new
empirical results land and after reflecting on them. It may disagree with
`thor-conjectures-human.md`.

Phrase entries as beliefs, predictions, falsifiers, and alternatives. Link to
results docs as backlinks rather than restating results here.

## Current conjectures

### A1: `<short title>`

- **Belief**: `<what the agent currently thinks>`
- **Origin**: `<which result or reasoning prompted this>`
- **Prediction**: `<what we'd see if right>`
- **Falsifier**: `<what would kill it>`
- **Tension with human**: `<does this disagree with a Cxx? how to distinguish?>`
- **Confidence**: `<low / medium / high>`

### A2: `<short title>`

- ...

## Reflection log (optional)

- `<date>`: after `<study>`, updated A1 because `<reason>`.
