# Results Synthesis (thor-results-synthesis.md)

Read role and rules: [Results Synthesis](thor-document-rules.md#results-synthesis).

Mid-level empirical synthesis for the primary results theme. This is the layer
between the compact [results summary](thor-results-summary.md) and the raw
[results ledger](thor-results-by-time.md): detailed enough to carry mechanism
interpretation, organised by question rather than by chronology.

This is an **empirical doc**. Record what the evidence shows and where it is
weak. Do not argue for or against a conjecture here — that belongs in the
conjecture files.

In a multi-topic project, name it `<topic>-results-synthesis.md`.

Current primary synthesis: `<one line naming the theme this doc covers>`

## Current Read

`<The best current understanding of this theme, in a few paragraphs. Lead with
what changed most recently and what it did to the story. State negatives as
plainly as positives — a result that killed a clean hypothesis is usually the
most valuable paragraph on the page.>`

## `<Empirical question 1>`

`<Organise by question, not by experiment. A good heading is the question the
reader has ("Is the effect prompt-side or answer-side?"), not the experiment
name that happened to address it.>`

`<For each question, say: what the evidence currently supports, how strong that
support is, what would overturn it, and which caveat constrains the next
experiment.>`

| Result | Read | Caveat |
| --- | --- | --- |
| `<finding>` | `<strong / modest / negative / control-sensitive / decision-critical>` | `<what limits this row>` |

## `<Empirical question 2>`

`<Repeat per question. Split a question into its own section once it accumulates
enough evidence to need its own caveats.>`

## Figures

`<Keep only figures that change interpretation or priority. A figure that is
merely recorded belongs with its study note or in the artifact store.>`

![`<takeaway-style caption>`](`<path>`)

## Open And Ambiguous

`<Where the story is still unresolved, and what would resolve it. Being explicit
here is what stops an ambiguous read hardening into an assumed result.>`

## Evidence Pointers

`<Link out rather than restating. Durable claims live in the claim-evidence map;
artifact coverage lives in the ledger; ranked plans live in the agenda.>`

- Claims: [claim-evidence map](thor-claim-evidence.md)
- Artifact coverage: [results ledger](thor-results-by-time.md)
- Next experiments: [experiment agenda](thor-next-steps.md)

---

*Update when results change the synthesis, even if they do not yet justify a
durable claim. If the repo accumulates several large empirical themes, keep this
file as the integrative index and move theme-specific detail into
`<theme>-results-synthesis.md` docs.*
